{
  "name": "ac-objectdiff",
  "author": "Mark Poepping (https://www.mmpro.de)",
  "license": "MIT",
  "repository": "mmpro/ac-objectdiff",
  "version": "0.1.8",
  "dependencies": {
    "lodash": "^4.17.11"
  },
  "devDependencies": {
    "eslint": "^5.15.1",
    "eslint-config-standard": "^12.x",
    "eslint-plugin-import": "^2.16.0",
    "eslint-plugin-node": "^8.x",
    "eslint-plugin-promise": "^4.x",
    "eslint-plugin-standard": "^4.x",
    "expect": "^24.x",
    "mocha": "^6.x",
    "mocha-jenkins-reporter": "github:mmpro/mocha-jenkins-reporter",
    "mocha-junit-reporter": "^1.18.0",
    "mocha-multi-reporters": "^1.1.4"
  },
  "scripts": {
    "test": "mocha --reporter spec",
    "test-jenkins": "JUNIT_REPORT_PATH=./report.xml mocha --bail --colors --reporter mocha-jenkins-reporter --reporter-options junit_report_name='ByteConverter'"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
